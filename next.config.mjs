/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['www.example.com', 'via.placeholder.com'], // tambahkan domain di sini
  },
};

export default nextConfig;
